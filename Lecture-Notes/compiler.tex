\chapter{Development of a Simple Compiler}

\begin{figure}[h] 
\centering
  \includegraphics[width=17.0cm]{Abbildungen/compiler.jpg}
\caption{A medieval compiler.}
\label{fig:compiler.png}
\end{figure}

\noindent
In this chapter, we construct a compiler that translates a fragment of the \mytt{C} language into \textsl{Java}
assembler. The fragment of the \mytt{C} language translated by the compiler is referred to as
\blue{\textsl{Integer}-\mytt{C}}, \index{Integer-C@\textsl{Integer}-\mytt{C}} because only the data type \mytt{int} is
available. Although it would be feasible to support additional data types, the extra effort would no longer be
proportionate to the educational benefit of the example. 

A compiler essentially consists of the following components:
\begin{enumerate}
\item The scanner reads the file to be translated and breaks it down into a sequence of tokens.
\item The parser reads the sequence of tokens and produces an abstract syntax tree as a result.

      We will generate both the scanner and the parser with \simtextsc{Lark} from a single grammar.
\item The type-checker checks the abstract syntax tree for type errors. Since the language
      \textsl{Integer}-\mytt{C} contains only a single data type, this phase is not needed for our compiler. 
\item The code generator then translates the parse tree into a sequence of assembler commands that conform to
      the syntax of \textsl{Jasmin}. 
\item This can be followed by an \emph{optimization phase}.   Due to time constraints we cannot cover this
      topic. 
\item The resulting assembler program can be translated into Java bytecode using \textsl{Jasmin}.
\item The Java bytecode is then executed by the \simtextsc{Jvm}, i.e.~the \textsl{Java} virtual machine.
\end{enumerate}
In compilers whose target code is a \simtextsc{Risc} assembler program, a code similar to \simtextsc{Jvm} code is
usually generated first. Such a code is referred to as \emph{intermediate code}. It then remains the task of a
so-called \emph{backend} to generate an assembler program for a given processor architecture. The most
challenging task here is to find a register allocation for the variables, such that as many variables as
possible can be held in registers.  


\section{The Programming Language \textsl{Integer}-\mytt{C}}
\begin{figure}[!ht]
  \begin{center}
  \fcolorbox{blue}{sepia}{
  \begin{minipage}[t]{12.5cm}
  \begin{eqnarray*}    
\textsl{program}      & \rightarrow & \;\textsl{function}\mytt{+} \\[0.2cm]
\textsl{function}     & \rightarrow & \quoted{int} \simtextsc{Id} \quoted{(} \textsl{paramList} \quoted{)} \quoted{\{} \textsl{decl}\,\mytt{+}\; \textsl{stmnt}\mytt{+} \;\squoted{\}} \\[0.2cm]
\textsl{paramList}    & \rightarrow & \;(\squoted{int}\, \simtextsc{Id}\; (\squoted{,}\, \quoted{int} \simtextsc{Id})\mytt{*})?  \\[0.2cm]
\textsl{decl}  & \rightarrow & \quoted{int} \simtextsc{Id} \quoted{;}  \\[0.2cm]
\textsl{stmnt}    & \rightarrow & \quoted{\{} \textsl{stmnt}\mytt{*} \quoted{\}}  \\[0.0cm]
                      & \mid        & \;\simtextsc{Id} \quoted{=} \textsl{expr} \;\squoted{;} \\[0.0cm]
                      & \mid        &  \quoted{if} \quoted{(} \textsl{boolExpr} \quoted{)} \textsl{stmnt} \\[0.0cm]
                     & \mid         &  \quoted{if} \quoted{(} \textsl{boolExpr} \quoted{)} \textsl{stmnt} \quoted{else} \textsl{stmnt} \\[0.0cm]
                     & \mid        &  \quoted{while} \quoted{(} \textsl{boolExpr} \quoted{)} \textsl{stmnt} \\[0.0cm]
                     & \mid        &  \quoted{return} \textsl{expr}\; \squoted{;} \\[0.0cm]
                     & \mid        &  \;\textsl{expr} \;\squoted{;}               \\[0.2cm]
   \textsl{boolExpr} & \rightarrow & \textsl{boolExpr} \;(\squoted{\&\&} \mid \squoted{||})\; \textsl{boolExpr} \\
         & \mid      &  \quoted{!} \textsl{boolExpr}             \\[0.0cm]
                     & \mid        &  \quoted{(} \textsl{boolExpr} \quoted{)}   \\[0.2cm]
         & \mid      &    \textsl{expr} \;(\squoted{==} \mid \squoted{!=} \mid \squoted{<=} \mid \squoted{>=} \mid \squoted{<} \mid \squoted{>})\;  \textsl{expr} \\[0.2cm]
 \textsl{expr} & \rightarrow & \textsl{expr} \;(\squoted{+} \mid \squoted{-} \mid \squoted{*} \mid \squoted{/} \mid \squoted{\%})\; \textsl{expr} \\[0.0cm]
     & \mid        &  \quoted{(} \textsl{expr} \quoted{)}                 \\[0.0cm]
     & \mid        &  \simtextsc{Number}                             \\[0.0cm]
     & \mid        &  \simtextsc{Id}\; (\squoted{(} (\textsl{expr}\; (\squoted{,}\; \textsl{expr})\mytt{*})?\;\squoted{)})?  
  \end{eqnarray*}
  \vspace*{-0.5cm}

  \end{minipage}}
  \end{center}
  \caption{An \simtextsc{Ebnf} grammar for \textsl{Integer}-\textsc{C}}
\label{fig:compiler.ebnf}
\end{figure}

\noindent
We now introduce the language \textsl{Integer}-\texttt{C}, which our compiler is intended to translate. In this
context, we also refer to it as the \emph{source language} of our compiler. Figure \ref{fig:compiler.ebnf} on
page \pageref{fig:compiler.ebnf} shows the grammar of the source language in Extended Backus-Naur Form
(\simtextsc{Ebnf}). The grammar for \textsl{Integer}-\texttt{C} uses the following terminals: 
\begin{enumerate}
\item \simtextsc{Id} is an abbreviation for \blue{Identifier} and represents a sequence of digits, letters, and underscores that begins with a letter. An \simtextsc{Id} designates either a variable or the name of a function.
\item \simtextsc{Number} stands for a sequence of digits interpreted as a decimal number.
\item In addition, there are a number of operators such as \texttt{"+"}, \texttt{"-"}, etc., as well as keywords like \texttt{"if"}, \texttt{"else"}, and \texttt{"while"}.
\end{enumerate}
According to the grammar provided above, a program is a list of functions. A function consists of the
declaration of its signature, followed by a list of declarations (\textsl{decl}) and commands (\textsl{stmt})
enclosed in curly braces.  The grammar shown
in Figure \ref{fig:compiler.ebnf} is ambiguous:
\pagebreak

\begin{enumerate}
\item The grammar has the \blue{Dangling-Else Problem}\index{dangling else problem}.  We will see that the
      \simtextsc{Lalr} parser generated by \simtextsc{Lark} resolves this problem in the way we want.
\item The rules for arithmetic and Boolean expressions do not specify the precedences and associativities of
      the operators.  We will encode this information into the grammar by \blue{stratifying} it, i.e.~by
      introducing one syntactical variable for every precedence level.
\end{enumerate}

\begin{figure}[!ht]
\centering
\begin{minted}[ frame         = lines, 
                framesep      = 0.3cm, 
                firstnumber   = 1,
                bgcolor       = sepia,
                labelposition = bottomline,
                numbers       = left,
                numbersep     = -0.2cm,
                xleftmargin   = 0.8cm,
                xrightmargin  = 0.8cm,
              ]{c}
    int sum(int n) {
        int s;
        s = 0;
        while (n != 0) {
            s = s + n;
            n = n - 1;    
        }
        return s;
    }  
    int main() {
        int n;
        n = 6 * 6;
        println(sum(n));
    }
\end{minted}
\vspace*{-0.3cm}
\caption{A simple \simtextsc{Integer}-\mytt{C} program to compute the sum
  $\displaystyle \sum\limits_{i=1}^{6 \cdot 6} i$.}
\label{fig:sum.c}
\end{figure}

\noindent
Figure \ref{fig:sum.c} shows a simple \simtextsc{Integer}-\texttt{C} program. The function $\textsl{sum}(n)$
calculates the sum 
\\[0.2cm]
\hspace*{1.3cm}
$\displaystyle\sum\limits_{i=1}^n i$
\\[0.2cm]
 and the function $\textsl{main}()$ calls the function \texttt{sum}
with the argument $6 \cdot 6$. The function \texttt{println} used in the program outputs its argument followed
by a newline. We assume that this function is predefined. 

\section{Developing the  Scanner and the Parser}
We construct both the scanner and the parser using \simtextsc{Lark}.  In contrast to parser generators like
\simtextsc{Ply}, which separate the specification of the scanner from the specification of the parser,
\simtextsc{Lark} reads a \emph{single} grammar that specifies both the tokens and the grammar rules.  Recall that
the names of terminals are written in upper case, while the names of syntactical variables are written in
lower case.  We build this grammar as a string: The part of the grammar that describes the scanner is collected in
the variable \mytt{terminal\_definitions}, while the grammar rules are collected in the variable \mytt{syntax}.
At the end, both strings are concatenated.  The notebook
\\[0.2cm]
\hspace*{1.3cm}
\href{https://github.com/karlstroetmann/Formal-Languages/blob/master/Python/Chapter-13/Compiler.ipynb}{\texttt{Python/Chapter-13/Compiler.ipynb}}
\\[0.2cm]
contains the complete compiler.  In this notebook, the strings \mytt{terminal\_definitions} and \mytt{syntax}
are built step by step.  For the presentation in these lecture notes, we have combined these steps.

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                fontsize     = \small,
                numbers      = left,
                numbersep    = -0.2cm,
                xleftmargin  = 0.8cm,
                xrightmargin = 0.8cm,
              ]{python3}
    from lark import Lark, Transformer, v_args
    from lark.exceptions import UnexpectedInput

    terminal_definitions = r"""
    NUMBER: /0|[1-9][0-9]*/

    EQ : "=="
    NE : "!="
    LE : "<="
    GE : ">="
    AND: "&&"
    OR : "||"

    COMMENT: /\/\*(.|\n)*?\*\//
           | /\/\/[^\n]*/
    %ignore COMMENT

    NAME: /[a-zA-Z][a-zA-Z0-9_]*/

    %ignore /[ \t\r\n]+/
    """
\end{minted}
\vspace*{-0.3cm}
\caption{The scanner for \textsl{Integer}-\mytt{C}.}
\label{fig:Compiler.ipynb:lex}
\end{figure}

\noindent
Figure \ref{fig:Compiler.ipynb:lex} on page \pageref{fig:Compiler.ipynb:lex} shows the specification of the
scanner.
\begin{enumerate}
\item Line 5 defines the token \mytt{NUMBER}, which specifies a natural number.  In \simtextsc{Lark}, a regular
      expression is enclosed in forward slashes.
\item Line 7--12 define the tokens for those operators that consist of more than one character.  Strictly
      speaking, these definitions are not necessary: A string that occurs literally inside a grammar rule, for
      example \quoted{==}, is automatically turned into an \blue{anonymous terminal}.  However, the name that
      \simtextsc{Lark} generates for an anonymous terminal built from a symbol like \quoted{==} has the form
      \mytt{\_\_ANON\_1}, and such a name is of little help when we inspect the token stream or read an error
      message.  If a terminal with the same definition has been declared explicitly, \simtextsc{Lark} uses this
      terminal instead of creating an anonymous one.  Hence, these declarations only serve to give the tokens
      readable names.  Note that a terminal that is defined by a string needs no escaping: We can simply write
      \quoted{||}.  Furthermore, \simtextsc{Lark} prefers longer strings, so \quoted{!=} is preferred over
      \quoted{!} and \quoted{<=} is preferred over \quoted{<}.

      The operators that consist of a single character need no declaration at all:  \simtextsc{Lark} gives
      the corresponding anonymous terminals names like \mytt{PLUS}, \mytt{LPAR}, or \mytt{SEMICOLON}.
\item Our version of \texttt{C} supports both single-line comments and multi-line comments.  These are
      defined by the token \mytt{COMMENT} in line 14 and 15, which shows that a terminal can be defined as an
      alternative of several regular expressions.
      \begin{enumerate}[(a)]
      \item Multi-line comments start with the string \quoted{/*} and end with the string \quoted{*/}.
            Note that we have to use the \emph{non-greedy} quantifier \quoted{*?}.  Otherwise, a line like
            \\[0.2cm]
            \hspace*{1.3cm}
            \mytt{/* blah */ a = 1; /* blub */}
            \\[0.2cm]
            would be recognized as a single comment.
      \item Single-line comments start with the string \quoted{//} and extend to the end of the line.
      \end{enumerate}
      As the forward slash is the delimiter of a regular expression in \simtextsc{Lark}, every occurrence of
      the character \quoted{/} inside a regular expression has to be escaped as \quoted{\symbol{92}/}.
      The directive \mytt{\%ignore} in line 16 tells the scanner to discard all comments, so that they never
      show up in the token stream.
\item The most interesting aspect is the implementation of keywords like \quoted{while} or \quoted{return}.
      Syntactically, all keywords are identifiers, i.e.~they are also matched by the regular expression defining
      the token \mytt{NAME} in line 18.  In the grammar of Figure \ref{fig:compiler.ebnf}, this token has been
      called \simtextsc{Id}.  \simtextsc{Lark} deals with keywords automatically:  If a string terminal
      like \quoted{if} is also matched by the regular expression of another terminal, then \simtextsc{Lark}
      first scans a \mytt{NAME} and afterwards checks whether the resulting string is one of the keywords.  If
      it is, the \blue{token type}\index{token type} is changed accordingly.  Because of this, the string
      \mytt{iffy} is scanned as a single \mytt{NAME} and not as the keyword \mytt{if} followed by the
      identifier \mytt{fy}.  Therefore, the keywords do not need to be declared as tokens.  They are simply
      written as strings inside the grammar rules.
\item Finally, line 20 specifies that white space is ignored.  \simtextsc{Lark} keeps track of line numbers
      and column numbers on its own:  every token $t$ provides the attributes \mytt{t.line} and \mytt{t.column},
      which are used in error messages.
\end{enumerate}

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                numbers      = left,
                numbersep    = 0.3cm,
                fontsize     = \footnotesize,
                xleftmargin  = 0.0cm,
                xrightmargin = 0.0cm,
              ]{python3}
    syntax = r"""
    program: function+

    function: "int" NAME "(" parameters ")" "{" declarations statements "}"

    parameters  : ("int" NAME ("," "int" NAME)*)?

    declarations: ("int" NAME ";")*

    statements  : statement*

    statement: "{" statements "}"                                -> block
             | NAME "=" expr ";"                                 -> assign
             | "if" "(" bool_expr ")" statement                  -> if_stmnt
             | "if" "(" bool_expr ")" statement "else" statement -> if_else_stmnt
             | "while" "(" bool_expr ")" statement               -> while_stmnt
             | "return" expr ";"                                 -> return_stmnt
             | expr ";"                                          -> expr_stmnt
    """
\end{minted}
\vspace*{-0.3cm}
\caption{Grammar for Integer-\texttt{C}, part 1: programs, functions, and statements.}
\label{fig:Compiler.ipynb-1}
\end{figure}

Now we are ready to present the grammar rules.  As \simtextsc{Lark} supports \simtextsc{Ebnf}, the grammar
shown in Figure \ref{fig:compiler.ebnf} can be used nearly unchanged.  There are only two things we have to take
care of: First, the rules for arithmetic and Boolean expressions have to be stratified.  Second, we have to attach
names to those alternatives of a rule that we want to distinguish when we build the abstract syntax tree.
Figure \ref{fig:Compiler.ipynb-1} on page \pageref{fig:Compiler.ipynb-1} shows the first part of the grammar.
\begin{enumerate}
\item Line 2 specifies that a \mytt{program} is a non-empty list of function definitions.
\item A \mytt{function} definition starts with the keyword \quoted{int} that is followed by the name of the
      function, an opening parenthesis, a list of the parameters of the function, a closing parenthesis,
      an opening brace, a list of declarations, a list of statements, and finally a closing brace.
      The strings \quoted{int}, \quoted{(}, \quoted{)}, \quoted{\{}, and \quoted{\}} are anonymous terminals.
      \simtextsc{Lark} removes anonymous string terminals from the parse tree.  Therefore, the node of the parse
      tree that represents a function has exactly four children:  the name of the function, its parameters, its
      declarations, and its statements.

      The initial version of the programming language \texttt{C}, as detailed in the book
      ``\emph{The C Programming Language}'' \cite{kernighan:1978}, required that all variable declarations
      precede any statements, prohibiting the mixing of declarations and statements. While this limitation was
      lifted in subsequent versions of the language, our implementation still enforces this restriction.
\item The syntactical variables \mytt{parameters}, \mytt{declarations}, and \mytt{statements} defined in line
      6, 8, and 10 describe lists.  Using the \simtextsc{Ebnf} operators \quoted{*} and \quoted{?}, one rule per
      list suffices.  Note that a parameter list may be empty and that different parameters are separated by
      commas, while every declaration is terminated by a semicolon.  The parse trees of \mytt{parameters}
      and \mytt{declarations} contain only the tokens of type \mytt{NAME}, since all other tokens occurring in
      these rules are anonymous terminals.
\item Line 12--18 define the syntactical variable \mytt{statement}.  The names given after the operator
      \quoted{->} are the names of the methods that later build the abstract syntax tree for the
      corresponding alternative.

      The two alternatives \mytt{if\_stmnt} and \mytt{if\_else\_stmnt} cause the \blue{dangling else}
      problem:  When the parser has read a statement of the form
      \\[0.2cm]
      \hspace*{1.3cm}
      \mytt{if (} \textsl{bool\_expr} \mytt{)} \textsl{statement}
      \\[0.2cm]
      and the next token is the keyword \quoted{else}, then the parser can either \emph{reduce} this statement or
      it can \emph{shift} the token \quoted{else}.  \simtextsc{Lark} resolves a shift-reduce conflict in favour of
      shifting.  This is exactly what we want:  the keyword \quoted{else} is attached to the innermost
      \quoted{if} that does not yet have an \quoted{else} part.  Hence, a statement of the form
      \\[0.2cm]
      \hspace*{1.3cm}
      \mytt{if (a == 1) if (b == 1) x = 2; else x = 3;}
      \\[0.2cm]
      is interpreted as
      \\[0.2cm]
      \hspace*{1.3cm}
      \mytt{if (a == 1) \{ if (b == 1) x = 2; else x = 3; \}}.
\end{enumerate}

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                fontsize     = \small,
                numbers      = left,
                numbersep    = -0.2cm,
                xleftmargin  = 0.8cm,
                xrightmargin = 0.8cm,
              ]{python3}
    syntax += r"""
    ?bool_expr: bool_expr "||" conjunction -> or_expr
              | conjunction

    ?conjunction: conjunction "&&" negation -> and_expr
                | negation

    ?negation: "!" negation -> not_expr
             | bool_atom

    ?bool_atom: expr "==" expr -> equal
              | expr "!=" expr -> unequal
              | expr "<=" expr -> less_or_equal
              | expr ">=" expr -> greater_or_equal
              | expr "<"  expr -> less
              | expr ">"  expr -> greater
              | "(" bool_expr ")"

    ?expr: expr "+" product -> add
         | expr "-" product -> subtract
         | product

    ?product: product "*" factor -> multiply
            | product "/" factor -> divide
            | product "%" factor -> modulo
            | factor

    ?factor: NUMBER                 -> number
           | NAME                   -> variable
           | NAME "(" arguments ")" -> function_call
           | "(" expr ")"

    arguments: (expr ("," expr)*)?
    """
\end{minted}
\vspace*{-0.3cm}
\caption{Grammar for Integer-\texttt{C}, part 2: Boolean and arithmetic expressions.}
\label{fig:Compiler.ipynb-2}
\end{figure}

\noindent
Figure \ref{fig:Compiler.ipynb-2} on page \pageref{fig:Compiler.ipynb-2} shows the grammar rules for Boolean and
arithmetic expressions.  The \simtextsc{Lalr} parser of \simtextsc{Lark} does not support precedence
declarations.  Therefore, we encode the precedences and associativities of the operators into the grammar by
introducing one syntactical variable per precedence level, as discussed in Section
\ref{section:operator-precedence}.
\begin{enumerate}
\item Line 2--17 define Boolean expressions:
      \begin{enumerate}[(a)]
      \item A \mytt{bool\_expr} is a list of \mytt{conjunction}s that are separated by the operator
            \quoted{||}, which represents \blue{logical or}.
      \item A \mytt{conjunction} is a list of \mytt{negation}s that are separated by the operator
            \quoted{\&\&}, which represents \blue{logical and}.  Hence, \quoted{\&\&} binds stronger than
            \quoted{||}.
      \item A \mytt{negation} is a \mytt{bool\_atom} that is preceded by an arbitrary number of negation
            operators \quoted{!}.
      \item A \mytt{bool\_atom} is either a comparison of two arithmetic expressions or a Boolean expression
            in parentheses.
      \end{enumerate}
      As the operators \quoted{||} and \quoted{\&\&} are left associative, the corresponding rules are
      \blue{left recursive}:  the syntactical variable on the left hand side of the rule occurs as the first
      symbol on the right hand side.  The operator \quoted{!} is right associative, since we want to interpret an
      expression of the form $\mytt{!!}a$ as  $\mytt{!}(\mytt{!}a)$.  Hence, the rule for \mytt{not\_expr}
      is \blue{right recursive}.  The comparison operators \quoted{==}, \quoted{!=}, \quoted{<=},
      \quoted{>=}, \quoted{<}, and \quoted{>} are non-associative, since our programming language disallows
      the chaining of comparison operators.  Therefore, the rules for \mytt{bool\_atom} are not recursive at
      all and an expression of the form $x < y < z$ is syntactically invalid.
\item The question mark that precedes the names \mytt{bool\_expr}, \mytt{conjunction}, \mytt{negation}, and
      \mytt{bool\_atom} tells \simtextsc{Lark} to \emph{inline} these syntactical variables:  If a node of the
      parse tree has only a single child and carries no name of its own, then this node is replaced by its
      child.  Without these question marks, the parse tree of the expression \mytt{x == 1} would contain a
      chain of four nodes.
\item Line 19--31 define arithmetic expressions in the same way:  The operators \quoted{+} and \quoted{-}
      have a lower precedence than the operators \quoted{*}, \quoted{/}, and \quoted{\%}.  As all of these
      operators are left associative, all rules are left recursive.  The last alternative of the rule for
      \mytt{factor} has no name, as a parenthesized expression should not create a node of its own.
\item Line 33 defines the arguments of a function call as a possibly empty list of expressions that are
      separated by commas.
\end{enumerate}

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                fontsize     = \small,
                numbers      = left,
                numbersep    = -0.2cm,
                xleftmargin  = 0.8cm,
                xrightmargin = 0.8cm,
              ]{python3}
    grammar = syntax + terminal_definitions

    import logging
    import lark

    lark.logger.setLevel(logging.WARNING)
    parser = Lark(grammar, parser='lalr', start='program', debug=True)
\end{minted}
\vspace*{-0.3cm}
\caption{Creating the parser.}
\label{fig:Compiler.ipynb-3}
\end{figure}

\noindent
Figure \ref{fig:Compiler.ipynb-3} on page \pageref{fig:Compiler.ipynb-3} shows how the parser is created.
Line 1 concatenates the grammar rules and the terminal definitions.  In line 7, the keyword argument
\mytt{parser='lalr'} selects the \simtextsc{Lalr} parser, while \mytt{start='program'} declares \mytt{program} to
be the start variable of our grammar.  By default, \simtextsc{Lark} reports neither shift-reduce conflicts nor
reduce-reduce conflicts.  Setting \mytt{debug} to \mytt{True} and lowering the log level of the logger of
\simtextsc{Lark} to \mytt{WARNING} in line 6 makes these conflicts visible.  For our grammar, \simtextsc{Lark}
reports only one conflict:
\\[0.2cm]
\hspace*{1.3cm}
\mytt{Shift/Reduce conflict for terminal ELSE: (resolving as shift)}
\\[0.2cm]
This is the conflict caused by the dangling else problem that has been discussed above.  All other conflicts have
been eliminated by stratifying the grammar.

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                fontsize     = \small,
                numbers      = left,
                numbersep    = -0.2cm,
                xleftmargin  = 0.8cm,
                xrightmargin = 0.8cm,
              ]{python3}
    @v_args(inline=True)
    class ASTBuilder(Transformer):
        # program and function definitions
        def program(self, *functions):
            return ('.',) + functions

        def function(self, name, parameters, declarations, statements):
            return ('fct', str(name), parameters, declarations, statements)

        def parameters(self, *names):
            return ('.',) + tuple(str(name) for name in names)

        def declarations(self, *names):
            return ('.',) + tuple(str(name) for name in names)

        def statements(self, *stmnts):
            return ('.',) + stmnts

        # statements
        def block(self, statements):
            return statements

        def assign(self, name, expr):
            return ('=', str(name), expr)

        def if_stmnt(self, condition, stmnt):
            return ('if', condition, stmnt)

        def if_else_stmnt(self, condition, then_stmnt, else_stmnt):
            return ('if-else', condition, then_stmnt, else_stmnt)

        def while_stmnt(self, condition, body):
            return ('while', condition, body)

        def return_stmnt(self, expr):
            return ('return', expr)

        def expr_stmnt(self, expr):
            return expr
\end{minted}
\vspace*{-0.3cm}
\caption{Construction of the abstract syntax tree, part 1.}
\label{fig:Compiler.ipynb-4}
\end{figure}

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                fontsize     = \small,
                numbers      = left,
                firstnumber  = last,
                numbersep    = -0.2cm,
                xleftmargin  = 0.8cm,
                xrightmargin = 0.8cm,
              ]{python3}
        # Boolean expressions
        def or_expr (self, lhs, rhs): return ('||', lhs, rhs)
        def and_expr(self, lhs, rhs): return ('&&', lhs, rhs)
        def not_expr(self, arg):      return ('!' , arg)

        def equal           (self, lhs, rhs): return ('==', lhs, rhs)
        def unequal         (self, lhs, rhs): return ('!=', lhs, rhs)
        def less_or_equal   (self, lhs, rhs): return ('<=', lhs, rhs)
        def greater_or_equal(self, lhs, rhs): return ('>=', lhs, rhs)
        def less            (self, lhs, rhs): return ('<' , lhs, rhs)
        def greater         (self, lhs, rhs): return ('>' , lhs, rhs)

        # arithmetical expressions
        def add     (self, lhs, rhs): return ('+', lhs, rhs)
        def subtract(self, lhs, rhs): return ('-', lhs, rhs)
        def multiply(self, lhs, rhs): return ('*', lhs, rhs)
        def divide  (self, lhs, rhs): return ('/', lhs, rhs)
        def modulo  (self, lhs, rhs): return ('%', lhs, rhs)

        def number(self, n):
            return ('Number', str(n))

        def variable(self, name):
            return str(name)

        def function_call(self, name, arguments):
            return ('call', str(name)) + arguments[1:]

        def arguments(self, *exprs):
            return ('.',) + exprs
\end{minted}
\vspace*{-0.3cm}
\caption{Construction of the abstract syntax tree, part 2.}
\label{fig:Compiler.ipynb-5}
\end{figure}

The parse tree that is computed by \simtextsc{Lark} is an object of class \mytt{lark.Tree}.  As the code
generator presented in the next section works on \blue{nested tuples}\index{nested tuple}, we have to convert the
parse tree into a nested tuple.  This is done by the class \mytt{ASTBuilder} that is shown in Figure
\ref{fig:Compiler.ipynb-4} on page \pageref{fig:Compiler.ipynb-4} and Figure \ref{fig:Compiler.ipynb-5} on page
\pageref{fig:Compiler.ipynb-5}.  This class is a subclass of the class \mytt{Transformer} provided by
\simtextsc{Lark}.
\begin{enumerate}
\item A transformer traverses the parse tree bottom-up.  For every node, it calls the method whose name is the
      name of the corresponding grammar rule, respectively the name that has been specified after the operator
      \quoted{->}.  The arguments of this method are the results that have already been computed for the
      children of the node.  The decorator \mytt{@v\_args(inline=True)} in line 1 specifies that these results
      are passed as separate arguments instead of being packed into a single list.
\item The abstract syntax trees are represented as follows:
      \begin{enumerate}[(a)]
      \item A list of $n$ items is represented as the nested tuple $(\squoted{.}, \textsl{item}_1, \cdots,
            \textsl{item}_n)$.  For example, a program consisting of the functions $f_1$, $\cdots$, $f_n$ is
            represented as $(\squoted{.}, f_1, \cdots, f_n)$.
      \item A function definition is represented as the nested tuple
            \\[0.2cm]
            \hspace*{1.3cm}
            $(\squoted{fct}, \textsl{name}, \textsl{parameters}, \textsl{variables}, \textsl{statements})$.
      \item A variable is represented as a string, while a number is represented as a pair of the form
            $(\squoted{Number}, \textsl{digits})$.
      \item All other nodes are represented as tuples whose first component is the operator.
            For example, the abstract syntax tree of an assignment is a tuple of the form
            $(\squoted{=}, \textsl{name}, \textsl{expr})$.
      \end{enumerate}
\item The tokens computed by \simtextsc{Lark} are objects of the class \mytt{lark.Token}, which is a subclass of
      \mytt{str}.  We convert these tokens into ordinary strings via the function \mytt{str}, so that the abstract
      syntax tree contains no objects that are specific to \simtextsc{Lark}.
\item A block is represented by the list of its statements, while an expression statement is represented by the
      expression itself.
\item In the method \mytt{function\_call}, the expression \mytt{arguments[1:]} discards the key \squoted{.} of the
      list of arguments.  Hence, a function call is represented as a tuple of the form
      \\[0.2cm]
      \hspace*{1.3cm}
      $(\squoted{call}, \textsl{name}, \textsl{arg}_1, \cdots, \textsl{arg}_n)$.
\end{enumerate}

\begin{figure}[!ht]
\centering
\begin{minted}[ frame        = lines,
                framesep     = 0.3cm,
                bgcolor      = sepia,
                gobble       = 4,
                fontsize     = \small,
                numbers      = left,
                numbersep    = 0.3cm,
                xleftmargin  = 0.0cm,
                xrightmargin = 0.0cm,
              ]{python3}
    ast_builder = ASTBuilder()

    def parse_program(program):
        try:
            parse_tree = parser.parse(program)
        except UnexpectedInput as error:
            print(f'Syntax error in line {error.line}, column {error.column}:')
            print(error.get_context(program))
            return None
        return ast_builder.transform(parse_tree)
\end{minted}
\vspace*{-0.3cm}
\caption{Parsing a program.}
\label{fig:Compiler.ipynb-6}
\end{figure}

\noindent
Finally, Figure \ref{fig:Compiler.ipynb-6} on page \pageref{fig:Compiler.ipynb-6} shows the function
\mytt{parse\_program}.  This function takes a string containing a program, parses it, and returns the
abstract syntax tree of this program.  If the program contains a syntax error, \simtextsc{Lark} raises an exception
of class \mytt{UnexpectedInput}.  This exception provides the attributes \mytt{line} and \mytt{column}, while the
method \mytt{get\_context} returns the line of the program that contains the error together with a marker that
points to the offending token.  In this case, \mytt{parse\_program} returns \mytt{None}.  For example, for the
erroneous program
\\[0.2cm]
\hspace*{1.3cm}
\mytt{int main() \{ x = 1 + ; \}}
\\[0.2cm]
the following error message is printed:
\\[0.2cm]
\hspace*{1.3cm}
\begin{minipage}{0.8\linewidth}
\begin{verbatim}
Syntax error in line 1, column 22:
int main() { x = 1 + ; }
                     ^
\end{verbatim}
\end{minipage}

\exerciseEng
Extend the parser of the notebook
\\[0.2cm]
\hspace*{1.3cm}
\href{https://github.com/karlstroetmann/Formal-Languages/blob/master/Python/Chapter-13/Compiler.ipynb}{\texttt{Python/Chapter-13/Compiler.ipynb}}
\\[0.2cm]
so that it supports the ternary \texttt{C}-operator for conditional expressions.  For example, in \texttt{C},
the following assignment using the ternary operator can be used to assign to the variable \mytt{max}
the maximum of the values of the variables \mytt{x} and \mytt{y}:
\\[0.2cm]
\hspace*{1.3cm}
\mytt{max = x > y ? x : y;}
\\[0.2cm]
Extend the grammar and the class \mytt{ASTBuilder} so that they can process the assignment shown above. \eox

%%% Local Variables: 
%%% mode: latex
%%% TeX-master: "formal-languages"
%%% End: 
