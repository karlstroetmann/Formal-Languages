how to construct graphs:

1. generate the deterministic automaton with:

   /usr/local/fsa/bin/fsa -r '[a *, b, a *]' ed.d

2. generate the tex file:

   /usr/local/fsa/bin/fsa -pstricks_tex ed.d > first.tex

3. edit tex file

  