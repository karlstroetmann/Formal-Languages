The attached file `03-Interpreter.ipynb` implements an interpreter for a simple programming language.
Currently, this program uses the library `ply`.  Your task is to rewrite this Jupyter notebook
so that `ply` is no longer needed.  Instead, you should use the parser generator `lark`.

I have used `lark`already in the file `01-Calculator.ipynb` which I have also attached.  In your traslation
of `03-Interpreter.ipynb` you should use `lark` in a similar way.  In particular, the parse should first create
an abstract syntax tree that is represented by nested tuples.

The file `03-Interpreter.ipynb` makes use of the noteboook `AST2Dot.ipynb` whichis used to represent the ADT graphically.
I have attached this file also.

It is important that the markdown cells are adapted.  The resulting notebook should have the same
level and quality of documentation as the original notebook.

The resulting notebook will be used as part of lecture on formal methods.  Hence It is important
that the resulting notebook is easily readable und well documented.

I need a JSON file as output.

